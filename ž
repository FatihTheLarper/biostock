import NavBar from "../components/navbar/navbar"

const About = () => {

  const navItems = [
    { name: "Home", route: "/" },
    { name: "Mealprep", route: "/mealprep" },
    { name: "Contact", route: "mailto:support@example.com" }
  ]

  return (
    <div className="w-full p-4 font-sans">

      <NavBar items={navitems}></NavBar>

    </div>
  )
}

export default About
